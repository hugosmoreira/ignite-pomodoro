import {
    createContext,
    ReactNode,
    useEffect,
    useReducer,
    useState,
  } from 'react'

interface CyclesContextType {
    
  }




export const CyclesContext = createContext({} as CyclesContextType)